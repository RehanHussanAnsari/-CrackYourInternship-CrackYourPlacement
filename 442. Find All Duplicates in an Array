class Solution {
public:
    vector<int> findDuplicates(vector<int>& nums) {
        vector<int>dup;
        unordered_map<int,int>hash;
        for(auto it:nums) hash[it]++;
        for(auto it:hash) if(it.second>1) dup.push_back(it.first);
        return dup;
    }
};
